import { translations as initial_select_model } from './initial-select-model'
import { translations as initial_select_provider } from './initial-select-provider'
import { translations as verify_model } from './verify-model'

export const translations = {
  ...initial_select_model,
  ...initial_select_provider,
  ...verify_model
}
