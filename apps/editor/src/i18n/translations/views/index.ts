import { translations as common } from './common'
import { translations as prompt } from './prompt'
import { translations as settings } from './settings'
import { translations as shared } from './shared'

export const translations = {
  ...common,
  ...prompt,
  ...settings,
  ...shared
}
