function add(a, b) {
  return a + b;
}

class Calculator {
  constructor() {
    this.value = 0;
  }

  add(n) {
    this.value += n;
  }
}

interface Item {
  id: string;
  name: string;
}

const double = (x) => {
  return x * 2;
}

const config = {
  env: "production",
  debug: false
};
